package 面试.算法.实习.字节.两个正序数组的中位数;

import java.util.PriorityQueue;

public class Solution {
    public double findMedianSortedArrays(int[] nums1, int[] nums2) {
        PriorityQueue<Integer> queue = new PriorityQueue<>((nums1.length+nums2.length)/2);
        for (int i : nums1) {
        }
        return 0.0;
    }
}
